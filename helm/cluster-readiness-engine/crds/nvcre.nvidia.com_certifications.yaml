---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: certifications.nvcre.nvidia.com
spec:
  group: nvcre.nvidia.com
  names:
    kind: Certification
    listKind: CertificationList
    plural: certifications
    singular: certification
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Certification is the Schema for the certifications API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              spec defines the desired state of Certification.
              The entire spec is immutable after creation: the controller never updates
              an active Workflow from an edited parent, so accepting edits would either
              silently ignore them or apply them only to later categories. To run with
              different inputs, delete the Certification and create a new one.
            properties:
              categories:
                description: |-
                  Categories are the list of certificate categories required for the Target.
                  The MaxItems bound keeps the CEL rules nested under each category (the
                  resources request/limit comparison) within the API server's validation
                  cost budget; the catalog defines far fewer entries than 64.
                items:
                  properties:
                    domain:
                      description: Domain is the high level Domain that the certificate
                        belongs to like training, inference etc
                      type: string
                    options:
                      description: |-
                        options provides per-category configuration overrides.
                        Fields set here take precedence over their CertificationSpec counterparts.
                      properties:
                        enableCheckpoint:
                          description: |-
                            enableCheckpoint enables checkpointing for training workloads.
                            When true, provisions a PVC for checkpoint storage, adds checkpoint config
                            (pvcName/maxRestarts), and enables --save/--load in training scripts.
                            When false (default), uses emptyDir and disables save/load.
                            Non-training entries ignore this field.
                          type: boolean
                        enableMNNVL:
                          description: |-
                            enableMNNVL enables Multi-Node NVLink (NCCL_MNNVL_ENABLE=1) for training
                            and communication workloads. Defaults to false (NCCL_MNNVL_ENABLE=0).
                            Enable this when running on platforms with multi-node NVLink connectivity
                            (e.g., GB300 NVL72). Can be overridden per-category via
                            categories[].options.enableMNNVL.
                          type: boolean
                        exitDurationMins:
                          description: |-
                            exitDurationMins sets the training duration in minutes. Maps to
                            EXIT_DURATION_MINS env var. Default: 30.
                          format: int32
                          minimum: 1
                          type: integer
                        gpusPerNode:
                          description: |-
                            gpusPerNode optionally overrides the number of GPUs per node used by catalog
                            workloads. If not specified, the controller derives the default from the GPU
                            architecture in target.nodeSelector (e.g., 4 for GB200/GB300, 8 for H100).
                            Use this field when your hardware has a non-standard GPU count per node.
                          format: int32
                          minimum: 1
                          type: integer
                        imagePullSecrets:
                          description: |-
                            imagePullSecrets is an optional list of references to secrets for pulling
                            container images used by catalog workloads. If not specified, the cluster's
                            default image pull configuration (e.g., ServiceAccount) is used.
                          items:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          type: array
                        maxBytes:
                          description: |-
                            maxBytes sets the maximum message size for NCCL tests (e.g., "16G", "32G").
                            Maps to the NCCL perf test `-e` flag. Default: "16G" (GB200/GB300 override: "32G").
                          pattern: ^[0-9]+(K|M|G|T)?$
                          type: string
                        maxConcurrent:
                          description: |-
                            maxConcurrent limits the number of simultaneously running jobs within
                            a Workflow. Useful for diagnose-mode parallel screening to avoid
                            fabric saturation. 0 means unlimited. Default: 0.
                          format: int32
                          type: integer
                        maxRestarts:
                          description: |-
                            maxRestarts sets the maximum number of checkpoint-based restarts for
                            training workloads. Maps to checkpoint.maxRestarts in the Job spec.
                            Only used when enableCheckpoint is true. Default: catalog-defined.
                          format: int32
                          minimum: 0
                          type: integer
                        maxSteps:
                          description: |-
                            maxSteps sets the maximum training steps. Maps to trainer.max_steps in the
                            NeMo config. Default: 50.
                          format: int32
                          minimum: -1
                          type: integer
                        measurementTimeout:
                          description: |-
                            measurementTimeout is the maximum time to wait after a Job succeeds for
                            measurement data (bandwidth, goodput, etc.) before failing threshold validation.
                            Accepts Go duration strings (e.g., "5m", "10m", "30m").
                            When not set, defaults to "5m".
                          pattern: ^([0-9]+(h|m|s|ms))+$
                          type: string
                        minGroupSize:
                          description: |-
                            minGroupSize sets the smallest group size at which diagnose's internal
                            bisection stops splitting. Groups at this size that fail become suspects.
                            Default: 2.
                          format: int32
                          minimum: 1
                          type: integer
                        mlnxPerNode:
                          description: |-
                            mlnxPerNode overrides the auto-detected Mellanox NIC count per node.
                            Used by platforms with InfiniBand or RoCE networking (Azure, OCI, TogetherAI).
                            If not specified, derived from GPU architecture and platform via the
                            catalog's gpu-defaults.yaml (e.g., 8 for most architectures, 2 for OCI L40s).
                          format: int32
                          minimum: 0
                          type: integer
                        nodesPerJob:
                          description: |-
                            nodesPerJob is the number of nodes per job for multi-node workloads.
                            When nil at both global and per-category level, the controller auto-selects:
                              - Entries with per-node-count configs (training): largest config <= matching nodes.
                              - All other entries: all matching nodes.
                            When set, clamped to min(nodesPerJob, matchingNodes).
                          format: int32
                          minimum: 1
                          type: integer
                        numCycles:
                          description: |-
                            numCycles sets the number of run cycles for NCCL tests. Each cycle runs
                            numIterations iterations and prints results separately.
                            Maps to the NCCL perf test `-N` flag. Default: 10.
                          format: int32
                          minimum: 1
                          type: integer
                        numIterations:
                          description: |-
                            numIterations sets the number of timed iterations per message size for NCCL tests.
                            Maps to the NCCL perf test `-n` flag. Default: 100.
                          format: int32
                          minimum: 1
                          type: integer
                        repeatCount:
                          description: |-
                            repeatCount sets the number of orchestration iterations for the Workflow.
                            Each iteration runs all groups and collects results. Multiple iterations
                            allow repeated testing for intermittent failures. Default: 1.
                          format: int32
                          minimum: 1
                          type: integer
                        resources:
                          description: |-
                            resources overrides the CPU and memory resources of training workload
                            containers. Training entries default to DGX-class sizing (limits:
                            cpu "128" / memory "800Gi"; requests: cpu "64" / memory "500Gi") — set
                            this on smaller nodes so training pods can schedule. Each of the four
                            values independently falls back to its default when unset. GPU count is
                            controlled by gpusPerNode. Non-training entries ignore this field.
                            Platform-specific catalog overrides (for example AWS with H100 GPUs)
                            may supersede these values.
                          properties:
                            limits:
                              description: limits overrides the container resource
                                limits.
                              properties:
                                cpu:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: cpu is the CPU quantity (e.g., "6",
                                    "1500m").
                                  maxLength: 32
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                  x-kubernetes-validations:
                                  - message: cpu must be a non-negative quantity
                                    rule: quantity(string(self)).compareTo(quantity("0"))
                                      >= 0
                                memory:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: memory is the memory quantity (e.g.,
                                    "48Gi").
                                  maxLength: 32
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                  x-kubernetes-validations:
                                  - message: memory must be a non-negative quantity
                                    rule: quantity(string(self)).compareTo(quantity("0"))
                                      >= 0
                              type: object
                            requests:
                              description: requests overrides the container resource
                                requests.
                              properties:
                                cpu:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: cpu is the CPU quantity (e.g., "6",
                                    "1500m").
                                  maxLength: 32
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                  x-kubernetes-validations:
                                  - message: cpu must be a non-negative quantity
                                    rule: quantity(string(self)).compareTo(quantity("0"))
                                      >= 0
                                memory:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: memory is the memory quantity (e.g.,
                                    "48Gi").
                                  maxLength: 32
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                  x-kubernetes-validations:
                                  - message: memory must be a non-negative quantity
                                    rule: quantity(string(self)).compareTo(quantity("0"))
                                      >= 0
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: requests.cpu must not exceed limits.cpu
                            rule: '!has(self.requests) || !has(self.limits) || !has(self.requests.cpu)
                              || !has(self.limits.cpu) || quantity(string(self.requests.cpu)).compareTo(quantity(string(self.limits.cpu)))
                              <= 0'
                          - message: requests.memory must not exceed limits.memory
                            rule: '!has(self.requests) || !has(self.limits) || !has(self.requests.memory)
                              || !has(self.limits.memory) || quantity(string(self.requests.memory)).compareTo(quantity(string(self.limits.memory)))
                              <= 0'
                        saveInterval:
                          description: |-
                            saveInterval sets the checkpoint save frequency in training steps.
                            NeMo 6: maps to --save-interval; NeMo 4: maps to every_n_train_steps.
                            Only used when enableCheckpoint is true. Default: 250.
                          format: int32
                          minimum: 1
                          type: integer
                        saveRetainInterval:
                          description: |-
                            saveRetainInterval retains checkpoints at multiples of this value,
                            deleting intermediate checkpoints (NeMo 6 only, --save-retain-interval).
                            The most recent checkpoint is always kept regardless.
                            Only used when enableCheckpoint is true. Default: 1000.
                          format: int32
                          minimum: 1
                          type: integer
                        saveTopK:
                          description: |-
                            saveTopK keeps only the top K checkpoints by monitored metric
                            (NeMo 4 only, save_top_k). Older checkpoints that aren't the best
                            are deleted to save storage.
                            Only used when enableCheckpoint is true. Default: 1.
                          format: int32
                          minimum: 1
                          type: integer
                        storageClassName:
                          description: |-
                            storageClassName is the StorageClass to use for PersistentVolumeClaim
                            dependencies created by catalog entries. If not specified, catalog entries
                            that require PVCs will use the cluster's default StorageClass.
                          type: string
                        storageSize:
                          description: |-
                            storageSize sets the PVC size for checkpoint storage (e.g., "10Ti", "500Gi").
                            Must be a valid Kubernetes resource quantity. Only used when enableCheckpoint
                            is true. Default: "10Ti".
                          pattern: ^([0-9]+(\.[0-9]+)?)(Ki|Mi|Gi|Ti|Pi|Ei)?$
                          type: string
                        testScale:
                          description: |-
                            testScale controls the orchestration strategy for NCCL communication tests.
                            Supported values:
                              - "intra-node": each node tested independently (nodesPerJob=1)
                              - "intra-rack": topology-partitioned per nvidia.com/gpu.clique
                              - "diagnose": adaptive fault isolation (topology-aware hierarchical group testing)
                              - "full-scale": all nodes in a single group (default)
                            Non-communication entries ignore this field.
                          enum:
                          - intra-node
                          - intra-rack
                          - diagnose
                          - full-scale
                          type: string
                        thresholds:
                          additionalProperties:
                            type: string
                          description: |-
                            thresholds defines performance thresholds as CEL expressions.
                            Keys are metric names (e.g., "busBandwidthGBps", "goodputRatio").
                            Values are CEL expressions using a `value` variable (float64).
                            Example: {"busBandwidthGBps": "value >= 900", "avgStepTimeSec": "value <= 3.0"}
                          type: object
                        timeoutPerJob:
                          description: |-
                            timeoutPerJob is the maximum time to wait for each job to complete.
                            Accepts Go duration strings (e.g., "1h", "30m", "2h30m").
                            When not set, defaults to "1h" (or "15m" for diagnose test scale).
                          pattern: ^([0-9]+(h|m|s|ms))+$
                          type: string
                      type: object
                    variant:
                      description: Variant is the lower level type such as nemotron,
                        deepseek, nccl etc.
                      type: string
                  required:
                  - domain
                  - variant
                  type: object
                maxItems: 64
                minItems: 1
                type: array
              enableCheckpoint:
                description: |-
                  enableCheckpoint enables checkpointing for training workloads.
                  When true, provisions a PVC for checkpoint storage, adds checkpoint config
                  (pvcName/maxRestarts), and enables --save/--load in training scripts.
                  When false (default), uses emptyDir and disables save/load.
                  Non-training entries ignore this field.
                type: boolean
              enableMNNVL:
                description: |-
                  enableMNNVL enables Multi-Node NVLink (NCCL_MNNVL_ENABLE=1) for training
                  and communication workloads. Defaults to false (NCCL_MNNVL_ENABLE=0).
                  Enable this when running on platforms with multi-node NVLink connectivity
                  (e.g., GB300 NVL72). Can be overridden per-category via
                  categories[].options.enableMNNVL.
                type: boolean
              exitDurationMins:
                description: |-
                  exitDurationMins sets the training duration in minutes. Maps to
                  EXIT_DURATION_MINS env var. Default: 30.
                format: int32
                minimum: 1
                type: integer
              gangScheduler:
                description: |-
                  gangScheduler opts every category's workload pods into a gang-aware
                  scheduler such as KAI Scheduler. When set, schedulerName is injected into
                  every pod template of every category's resolved TrainingRuntime and the
                  queue label is applied, so the scheduler holds all pods until the full
                  gang can be placed. Applied after catalog and platform overrides resolve,
                  so it also replaces the scheduler a catalog entry hardcodes.
                properties:
                  queue:
                    description: |-
                      queue is the scheduler queue to submit the workload to.
                      Defaults to "default-queue" if not specified.
                      When non-empty, must be a valid Kubernetes label value: at most 63 characters,
                      beginning and ending with an alphanumeric character, and containing only
                      alphanumerics, hyphens, underscores, or dots.
                    maxLength: 63
                    pattern: ^$|^[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?$
                    type: string
                  schedulerName:
                    description: schedulerName is the name of the gang-aware scheduler
                      to use (e.g. "kai-scheduler").
                    minLength: 1
                    type: string
                required:
                - schedulerName
                type: object
              gpusPerNode:
                description: |-
                  gpusPerNode optionally overrides the number of GPUs per node used by catalog
                  workloads. If not specified, the controller derives the default from the GPU
                  architecture in target.nodeSelector (e.g., 4 for GB200/GB300, 8 for H100).
                  Use this field when your hardware has a non-standard GPU count per node.
                format: int32
                minimum: 1
                type: integer
              imagePullSecrets:
                description: |-
                  imagePullSecrets is an optional list of references to secrets for pulling
                  container images used by catalog workloads. If not specified, the cluster's
                  default image pull configuration (e.g., ServiceAccount) is used.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              maxBytes:
                description: |-
                  maxBytes sets the maximum message size for NCCL tests (e.g., "16G", "32G").
                  Maps to the NCCL perf test `-e` flag. Default: "16G" (GB200/GB300 override: "32G").
                pattern: ^[0-9]+(K|M|G|T)?$
                type: string
              maxConcurrent:
                description: |-
                  maxConcurrent limits the number of simultaneously running jobs within
                  a Workflow. Useful for diagnose-mode parallel screening to avoid
                  fabric saturation. 0 means unlimited. Default: 0.
                format: int32
                type: integer
              maxRestarts:
                description: |-
                  maxRestarts sets the maximum number of checkpoint-based restarts for
                  training workloads. Maps to checkpoint.maxRestarts in the Job spec.
                  Only used when enableCheckpoint is true. Default: catalog-defined.
                format: int32
                minimum: 0
                type: integer
              maxSteps:
                description: |-
                  maxSteps sets the maximum training steps. Maps to trainer.max_steps in the
                  NeMo config. Default: 50.
                format: int32
                minimum: -1
                type: integer
              measurementTimeout:
                description: |-
                  measurementTimeout is the maximum time to wait after a Job succeeds for
                  measurement data (bandwidth, goodput, etc.) before failing threshold validation.
                  Accepts Go duration strings (e.g., "5m", "10m", "30m").
                  When not set, defaults to "5m".
                pattern: ^([0-9]+(h|m|s|ms))+$
                type: string
              minGroupSize:
                description: |-
                  minGroupSize sets the smallest group size at which diagnose's internal
                  bisection stops splitting. Groups at this size that fail become suspects.
                  Default: 2.
                format: int32
                minimum: 1
                type: integer
              mlnxPerNode:
                description: |-
                  mlnxPerNode overrides the auto-detected Mellanox NIC count per node.
                  Used by platforms with InfiniBand or RoCE networking (Azure, OCI, TogetherAI).
                  If not specified, derived from GPU architecture and platform via the
                  catalog's gpu-defaults.yaml (e.g., 8 for most architectures, 2 for OCI L40s).
                format: int32
                minimum: 0
                type: integer
              nodesPerJob:
                description: |-
                  nodesPerJob is the number of nodes per job for multi-node workloads.
                  When nil at both global and per-category level, the controller auto-selects:
                    - Entries with per-node-count configs (training): largest config <= matching nodes.
                    - All other entries: all matching nodes.
                  When set, clamped to min(nodesPerJob, matchingNodes).
                format: int32
                minimum: 1
                type: integer
              numCycles:
                description: |-
                  numCycles sets the number of run cycles for NCCL tests. Each cycle runs
                  numIterations iterations and prints results separately.
                  Maps to the NCCL perf test `-N` flag. Default: 10.
                format: int32
                minimum: 1
                type: integer
              numIterations:
                description: |-
                  numIterations sets the number of timed iterations per message size for NCCL tests.
                  Maps to the NCCL perf test `-n` flag. Default: 100.
                format: int32
                minimum: 1
                type: integer
              priorityClassName:
                description: |-
                  priorityClassName is set as spec.priorityClassName on every pod template
                  of every category's resolved TrainingRuntime, so the scheduler orders the
                  certification's pods against other work by that PriorityClass and, where it
                  preempts, preempts them by it. Applied after catalog and platform
                  overrides resolve, and independent of gangScheduler: pod priority is read
                  by the default scheduler and by gang schedulers alike. The PriorityClass
                  must exist in the cluster, or the pods are rejected at admission. Unset
                  leaves the pod templates as rendered.
                maxLength: 253
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              repeatCount:
                description: |-
                  repeatCount sets the number of orchestration iterations for the Workflow.
                  Each iteration runs all groups and collects results. Multiple iterations
                  allow repeated testing for intermittent failures. Default: 1.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: |-
                  resources overrides the CPU and memory resources of training workload
                  containers. Training entries default to DGX-class sizing (limits:
                  cpu "128" / memory "800Gi"; requests: cpu "64" / memory "500Gi") — set
                  this on smaller nodes so training pods can schedule. Each of the four
                  values independently falls back to its default when unset. GPU count is
                  controlled by gpusPerNode. Non-training entries ignore this field.
                  Platform-specific catalog overrides (for example AWS with H100 GPUs)
                  may supersede these values.
                properties:
                  limits:
                    description: limits overrides the container resource limits.
                    properties:
                      cpu:
                        anyOf:
                        - type: integer
                        - type: string
                        description: cpu is the CPU quantity (e.g., "6", "1500m").
                        maxLength: 32
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: cpu must be a non-negative quantity
                          rule: quantity(string(self)).compareTo(quantity("0")) >=
                            0
                      memory:
                        anyOf:
                        - type: integer
                        - type: string
                        description: memory is the memory quantity (e.g., "48Gi").
                        maxLength: 32
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: memory must be a non-negative quantity
                          rule: quantity(string(self)).compareTo(quantity("0")) >=
                            0
                    type: object
                  requests:
                    description: requests overrides the container resource requests.
                    properties:
                      cpu:
                        anyOf:
                        - type: integer
                        - type: string
                        description: cpu is the CPU quantity (e.g., "6", "1500m").
                        maxLength: 32
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: cpu must be a non-negative quantity
                          rule: quantity(string(self)).compareTo(quantity("0")) >=
                            0
                      memory:
                        anyOf:
                        - type: integer
                        - type: string
                        description: memory is the memory quantity (e.g., "48Gi").
                        maxLength: 32
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: memory must be a non-negative quantity
                          rule: quantity(string(self)).compareTo(quantity("0")) >=
                            0
                    type: object
                type: object
                x-kubernetes-validations:
                - message: requests.cpu must not exceed limits.cpu
                  rule: '!has(self.requests) || !has(self.limits) || !has(self.requests.cpu)
                    || !has(self.limits.cpu) || quantity(string(self.requests.cpu)).compareTo(quantity(string(self.limits.cpu)))
                    <= 0'
                - message: requests.memory must not exceed limits.memory
                  rule: '!has(self.requests) || !has(self.limits) || !has(self.requests.memory)
                    || !has(self.limits.memory) || quantity(string(self.requests.memory)).compareTo(quantity(string(self.limits.memory)))
                    <= 0'
              saveInterval:
                description: |-
                  saveInterval sets the checkpoint save frequency in training steps.
                  NeMo 6: maps to --save-interval; NeMo 4: maps to every_n_train_steps.
                  Only used when enableCheckpoint is true. Default: 250.
                format: int32
                minimum: 1
                type: integer
              saveRetainInterval:
                description: |-
                  saveRetainInterval retains checkpoints at multiples of this value,
                  deleting intermediate checkpoints (NeMo 6 only, --save-retain-interval).
                  The most recent checkpoint is always kept regardless.
                  Only used when enableCheckpoint is true. Default: 1000.
                format: int32
                minimum: 1
                type: integer
              saveTopK:
                description: |-
                  saveTopK keeps only the top K checkpoints by monitored metric
                  (NeMo 4 only, save_top_k). Older checkpoints that aren't the best
                  are deleted to save storage.
                  Only used when enableCheckpoint is true. Default: 1.
                format: int32
                minimum: 1
                type: integer
              storageClassName:
                description: |-
                  storageClassName is the StorageClass to use for PersistentVolumeClaim
                  dependencies created by catalog entries. If not specified, catalog entries
                  that require PVCs will use the cluster's default StorageClass.
                type: string
              storageSize:
                description: |-
                  storageSize sets the PVC size for checkpoint storage (e.g., "10Ti", "500Gi").
                  Must be a valid Kubernetes resource quantity. Only used when enableCheckpoint
                  is true. Default: "10Ti".
                pattern: ^([0-9]+(\.[0-9]+)?)(Ki|Mi|Gi|Ti|Pi|Ei)?$
                type: string
              target:
                description: target specifies which nodes to include in the orchestration.
                properties:
                  includeUnschedulable:
                    description: |-
                      includeUnschedulable keeps nodes marked unschedulable (cordoned) in the
                      target set instead of dropping them. Use it to test a node that was taken
                      out of service and has to prove itself before it is returned: the node
                      stays cordoned for the whole run, so nothing else can land on it.

                      Workload pods then also tolerate the node.kubernetes.io/unschedulable
                      taint, and the default node health check, which otherwise records a cordon
                      during the run as a hardware failure, is not applied. A nodeHealthMonitor
                      on the job template that is exactly that cordon check is dropped for the
                      same reason; any other expression is kept as written.
                    type: boolean
                  matchExpressions:
                    description: |-
                      matchExpressions is a list of node selector requirements.
                      Supports In, NotIn, Exists, DoesNotExist operators.
                      All expressions must match (AND with nodeSelector).
                    items:
                      description: |-
                        A node selector requirement is a selector that contains values, a key, and an operator
                        that relates the key and values.
                      properties:
                        key:
                          description: The label key that the selector applies to.
                          type: string
                        operator:
                          description: |-
                            Represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                          type: string
                        values:
                          description: |-
                            An array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. If the operator is Gt or Lt, the values
                            array must have a single element, which will be interpreted as an integer.
                            This array is replaced during a strategic merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  nodeNames:
                    description: |-
                      nodeNames explicitly lists nodes to include.
                      If specified, nodeSelector is ignored.
                    items:
                      type: string
                    type: array
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector selects nodes by label key-value pairs.
                    type: object
                  taintSelectors:
                    description: |-
                      taintSelectors selects nodes that have ALL matching taints.
                      A node must have every taint in this list to be included.

                      The controller auto-injects matching tolerations onto the workload pods so
                      they can schedule onto the selected tainted nodes. Setting this field is
                      the supported way to run training workloads (e.g. TrainJob) on tainted
                      nodes. When taintSelectors is set, the explicit tolerations replace the
                      blanket Operator: Exists toleration that MPI workloads (NCCL tests)
                      otherwise receive — declared selectors always win.
                    items:
                      description: TaintSelector selects nodes by taint.
                      properties:
                        effect:
                          description: |-
                            effect is the taint effect to match (NoSchedule, PreferNoSchedule, NoExecute).
                            If empty, matches any effect.
                          type: string
                        key:
                          description: key is the taint key to match.
                          minLength: 1
                          type: string
                        value:
                          description: value is the taint value to match. If empty,
                            matches any value.
                          type: string
                      required:
                      - key
                      type: object
                    type: array
                type: object
              testScale:
                description: |-
                  testScale controls the orchestration strategy for NCCL communication tests.
                  Supported values:
                    - "intra-node": each node tested independently (nodesPerJob=1)
                    - "intra-rack": topology-partitioned per nvidia.com/gpu.clique
                    - "diagnose": adaptive fault isolation (topology-aware hierarchical group testing)
                    - "full-scale": all nodes in a single group (default)
                  Non-communication entries ignore this field.
                enum:
                - intra-node
                - intra-rack
                - diagnose
                - full-scale
                type: string
              thresholds:
                additionalProperties:
                  type: string
                description: |-
                  thresholds defines performance thresholds as CEL expressions.
                  Keys are metric names (e.g., "busBandwidthGBps", "goodputRatio").
                  Values are CEL expressions using a `value` variable (float64).
                  Example: {"busBandwidthGBps": "value >= 900", "avgStepTimeSec": "value <= 3.0"}
                type: object
              timeoutPerJob:
                description: |-
                  timeoutPerJob is the maximum time to wait for each job to complete.
                  Accepts Go duration strings (e.g., "1h", "30m", "2h30m").
                  When not set, defaults to "1h" (or "15m" for diagnose test scale).
                pattern: ^([0-9]+(h|m|s|ms))+$
                type: string
            required:
            - categories
            - target
            type: object
            x-kubernetes-validations:
            - message: spec is immutable after creation
              rule: self == oldSelf
          status:
            description: status defines the observed state of Certification
            properties:
              categoryStatuses:
                description: categoryStatuses tracks the status of each certification
                  category.
                items:
                  description: CertificationCategoryStatus tracks the status of a
                    single certification category.
                  properties:
                    domain:
                      description: domain is the category domain.
                      type: string
                    failedNodesRef:
                      description: |-
                        failedNodesRef references the ConfigMap holding this category's failed-nodes
                        list (name, reason, message). Copied from the Workflow.
                      properties:
                        apiGroup:
                          description: |-
                            APIGroup is the group for the resource being referenced.
                            If APIGroup is not specified, the specified Kind must be in the core API group.
                            For any other third-party types, APIGroup is required.
                          type: string
                        kind:
                          description: Kind is the type of resource being referenced
                          type: string
                        name:
                          description: Name is the name of resource being referenced
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                      x-kubernetes-map-type: atomic
                    status:
                      description: |-
                        status is the current status of this category.
                        Values: "Pending", "InProgress", "Succeeded", "Failed"
                      type: string
                    succeededNodesRef:
                      description: |-
                        succeededNodesRef references the ConfigMap holding this category's
                        succeeded-nodes list (comma-separated node names). Copied from the Workflow.
                      properties:
                        apiGroup:
                          description: |-
                            APIGroup is the group for the resource being referenced.
                            If APIGroup is not specified, the specified Kind must be in the core API group.
                            For any other third-party types, APIGroup is required.
                          type: string
                        kind:
                          description: Kind is the type of resource being referenced
                          type: string
                        name:
                          description: Name is the name of resource being referenced
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                      x-kubernetes-map-type: atomic
                    variant:
                      description: variant is the category variant.
                      type: string
                    workflowRef:
                      description: workflowRef references the Workflow created for
                        this category.
                      properties:
                        name:
                          description: name is the name of the Workflow resource.
                          type: string
                        namespace:
                          description: namespace is the namespace of the Workflow
                            resource.
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - domain
                  - status
                  - variant
                  type: object
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the Certification resource.

                  Condition types:
                  - "InProgress": the Certification is currently running
                  - "Succeeded": the Certification completed successfully
                  - "Failed": the Certification has failed

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
